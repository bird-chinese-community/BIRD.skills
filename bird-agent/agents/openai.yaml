interface:
  display_name: "BIRD Agent"
  short_description: "Write, validate, format, and debug BIRD routing daemon configs."
  brand_color: "#3B82F6"

policy:
  allow_implicit_invocation: true

dependencies:
  tools: []
